# Persisted sklearn joblib detector produced offline (optional; sample auto-trained if absent)
ADVERSCAN_DETECTOR_ARTIFACT=artifacts/detector.joblib

# Victim checkpoint (``.pth`` /``.th``) for CIFAR-10–trained ResNet-18 (either variable works):
ADVERSCAN_VICTIM_CHECKPOINT=

ADVERSCAN_CIFAR10_RESNET18=

